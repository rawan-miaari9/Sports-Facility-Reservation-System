import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL('https://localhost:3000'),
  title:  'Athletic Hub',
   
 openGraph: {
  description: 'Athletic Hub simplifies booking sports facilities by letting customers reserve football fields, basketball courts, and more online. With a user-friendly dashboard, customers can manage reservations and view history, while administrators efficiently handle facilities, users, and scheduling through a powerful management dashboard.'
}

}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}